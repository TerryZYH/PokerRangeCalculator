# 📁 项目结构说明

## 🎯 目录结构

```
po4/
├── 📄 README.md                    # 项目主页和快速开始
├── 📄 CHANGELOG.md                 # 版本更新记录
├── 📄 PROJECT_STRUCTURE.md         # 本文件 - 项目结构说明
├── 📄 package.json                 # Node.js 依赖配置
├── 📄 .gitignore                   # Git 忽略规则
│
├── 📚 docs/                        # 文档目录
│   ├── README.md                   # 文档导航
│   ├── USER_GUIDE.md               # 用户使用手册（操作指南）
│   ├── TECHNICAL_DESIGN.md         # 技术设计文档（架构和实现）
│   ├── PRODUCT_DESIGN.md           # 产品设计文档（需求和功能）
│   ├── DEVELOPMENT.md              # 开发指南（环境配置和部署）
│   └── AI_INTEGRATION.md           # AI 集成说明（LangGraph + Azure OpenAI）
│
├── 🔧 scripts/                     # 管理脚本目录
│   ├── README.md                   # 脚本使用说明
│   ├── start.sh                    # ⭐ 启动完整服务（前端 + 后端）
│   ├── stop.sh                     # ⭐ 停止所有服务
│   ├── restart.sh                  # ⭐ 重启服务
│   ├── status.sh                   # ⭐ 查看服务状态
│   └── dev-frontend.sh             # 仅启动前端开发服务
│
├── 💻 src/                         # 前端源码
│   ├── components/                 # React 组件
│   │   ├── AIChat.tsx              # AI 聊天组件
│   │   ├── HandMatrix.tsx          # 手牌矩阵组件
│   │   ├── HandCell.tsx            # 单个手牌单元格
│   │   ├── ProbabilityDisplay.tsx  # 概率显示组件
│   │   ├── RangeSelector.tsx       # 范围选择器
│   │   └── BatchOperations.tsx     # 批量操作组件
│   ├── data/                       # 数据和配置
│   │   ├── presetRanges.ts         # 预设范围数据
│   │   └── ranks.ts                # 牌面等级定义
│   ├── services/                   # 服务层
│   │   └── api.ts                  # 后端 API 调用服务
│   ├── types/                      # TypeScript 类型定义
│   │   └── index.ts                # 类型接口
│   ├── utils/                      # 工具函数
│   │   ├── handGenerator.ts        # 手牌矩阵生成
│   │   ├── probability.ts          # 概率计算
│   │   └── storage.ts              # 本地存储
│   ├── App.tsx                     # 主应用组件
│   ├── main.tsx                    # React 应用入口
│   └── index.css                   # 全局样式
│
├── 🔙 backend/                     # 后端服务
│   ├── app/                        # FastAPI 应用
│   │   ├── config/                 # 配置模块
│   │   │   └── settings.py         # 环境变量和配置
│   │   ├── models/                 # 数据模型
│   │   │   └── schemas.py          # Pydantic 数据模型
│   │   ├── routes/                 # API 路由
│   │   │   ├── chat.py             # 聊天相关接口
│   │   │   └── range.py            # 范围分析接口
│   │   ├── services/               # 业务服务
│   │   │   ├── llm_service.py      # LLM 服务（Azure OpenAI）
│   │   │   └── poker_agent.py      # 扑克 AI Agent（LangGraph）
│   │   └── main.py                 # FastAPI 应用入口
│   ├── venv/                       # Python 虚拟环境（自动创建）
│   ├── .env                        # 环境变量配置（需手动配置）
│   ├── .env.example                # 环境变量配置示例
│   ├── requirements.txt            # Python 依赖
│   ├── Dockerfile                  # 后端 Docker 镜像
│   └── start.sh                    # 后端独立启动脚本
│
├── 📦 public/                      # 静态资源
│   └── vite.svg                    # Vite 图标
│
├── 📝 logs/                        # 日志目录（自动创建，已忽略）
│   ├── backend.log                 # 后端运行日志
│   └── frontend.log                # 前端运行日志
│
├── 🔢 .pids/                       # 进程 ID 目录（自动创建，已忽略）
│   ├── backend.pid                 # 后端进程 PID
│   └── frontend.pid                # 前端进程 PID
│
├── 🐳 docker-compose.yml           # Docker Compose 配置
├── 🐳 Dockerfile.frontend          # 前端 Docker 镜像
├── 📄 index.html                   # HTML 入口文件
├── ⚙️  vite.config.ts              # Vite 构建配置
├── ⚙️  tailwind.config.js          # Tailwind CSS 配置
├── ⚙️  tsconfig.json               # TypeScript 配置
├── ⚙️  tsconfig.node.json          # TypeScript Node 配置
└── ⚙️  postcss.config.js           # PostCSS 配置
```

---

## 🚀 快速使用

### 启动服务
```bash
# 启动完整服务（推荐）
./scripts/start.sh

# 仅启动前端（无 AI 功能）
./scripts/dev-frontend.sh
```

### 管理服务
```bash
# 查看状态
./scripts/status.sh

# 停止服务
./scripts/stop.sh

# 重启服务
./scripts/restart.sh
```

### 查看日志
```bash
# 实时查看日志
tail -f logs/backend.log logs/frontend.log

# 查看最近日志
tail -50 logs/backend.log
```

---

## 📂 目录说明

### `docs/` - 文档目录
所有项目文档集中在此，按受众分类：
- **用户** → `USER_GUIDE.md`
- **开发者** → `TECHNICAL_DESIGN.md` + `DEVELOPMENT.md`
- **产品/设计** → `PRODUCT_DESIGN.md`
- **AI 功能** → `AI_INTEGRATION.md`

### `scripts/` - 脚本目录
统一管理所有启动、停止、状态检查脚本。每个脚本都有详细的：
- ✅ 依赖检查
- ✅ 端口冲突检测
- ✅ 健康检查
- ✅ 友好的输出提示

### `src/` - 前端源码
模块化的 React + TypeScript 应用：
- `components/` - 可复用的 UI 组件
- `data/` - 数据定义和配置
- `services/` - API 调用服务
- `types/` - TypeScript 类型定义
- `utils/` - 纯函数工具

### `backend/` - 后端服务
基于 FastAPI + LangGraph 的 AI 后端：
- `app/config/` - 配置管理（环境变量）
- `app/models/` - 数据模型（Pydantic）
- `app/routes/` - API 路由（RESTful）
- `app/services/` - 业务逻辑（LLM、Agent）

### `logs/` - 日志目录
服务运行时自动创建，包含：
- `backend.log` - 后端日志（包括 AI 调用日志）
- `frontend.log` - 前端 Vite 开发服务器日志

已在 `.gitignore` 中忽略，不会提交到版本控制。

### `.pids/` - 进程 ID 目录
脚本自动管理的进程 ID 文件，用于：
- 启动时检查进程是否存在
- 停止时准确找到并终止进程
- 状态检查时显示进程信息

已在 `.gitignore` 中忽略。

---

## 🔧 配置文件

### 前端配置
- `vite.config.ts` - Vite 构建工具配置
- `tailwind.config.js` - Tailwind CSS 样式配置
- `tsconfig.json` - TypeScript 编译配置
- `postcss.config.js` - PostCSS 处理配置
- `package.json` - Node.js 依赖和脚本

### 后端配置
- `backend/.env` - 环境变量（API Key 等敏感信息）
- `backend/requirements.txt` - Python 依赖列表
- `backend/app/config/settings.py` - 应用配置管理

### Docker 配置
- `docker-compose.yml` - 容器编排配置
- `Dockerfile.frontend` - 前端镜像构建
- `backend/Dockerfile` - 后端镜像构建

---

## 📚 相关文档

- [README.md](./README.md) - 项目主页
- [docs/README.md](./docs/README.md) - 文档导航
- [scripts/README.md](./scripts/README.md) - 脚本使用说明
- [CHANGELOG.md](./CHANGELOG.md) - 版本更新记录

---

## 💡 开发建议

### 前端开发
1. 使用 `./scripts/dev-frontend.sh` 快速启动
2. 代码在 `src/` 目录下修改
3. Vite 支持热模块替换（HMR），修改即可看到效果

### 后端开发
1. 激活虚拟环境：`cd backend && source venv/bin/activate`
2. 使用 `uvicorn app.main:app --reload` 启动（支持热重载）
3. 访问 `http://localhost:8000/docs` 查看 API 文档

### 全栈开发
1. 使用 `./scripts/start.sh` 同时启动前后端
2. 使用 `./scripts/status.sh` 随时检查服务状态
3. 使用 `tail -f logs/*.log` 实时查看日志

---

**项目结构清晰，文档齐全，开发愉快！** 🎉

